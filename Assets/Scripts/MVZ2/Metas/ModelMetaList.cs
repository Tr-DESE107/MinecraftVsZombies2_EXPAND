#nullable enable // autogenerated

using System.Collections.Generic;
using System.Xml;

namespace MVZ2.Metas
{
    public class ModelMetaList
    {
        public ModelMeta[] metas;

        public ModelMetaList(ModelMeta[] metas)
        {
            this.metas = metas;
        }

        public static ModelMetaList FromXmlNode(XmlNode node, string defaultNsp)
        {
            var metas = new List<ModelMeta>();
            for (int i = 0; i < node.ChildNodes.Count; i++)
            {
                var meta = ModelMeta.FromXmlNode(node.ChildNodes[i], defaultNsp);
                if (meta != null)
                    metas.Add(meta);
            }
            return new ModelMetaList(metas.ToArray());
        }
    }
}
