#nullable enable // autogenerated

using MVZ2Logic.Level;
using PVZEngine;
using PVZEngine.Entities;
using PVZEngine.Level;

namespace MVZ2Logic.Entities
{
    [PropertyRegistryRegion(PropertyRegions.entity)]
    public static class LogicProjectileProps
    {
        private static PropertyMeta<T> Get<T>(string name, T? defaultValue = default, params string[] obsoleteNames)
        {
            return new PropertyMeta<T>(name, defaultValue, obsoleteNames);
        }

        #region 锁定目标
        public static readonly PropertyMeta<EntityID> LOCKED_TARGET_ID = Get<EntityID>("locked_target_id");
        public static EntityID? GetLockedTargetID(this Entity entity)
        {
            return entity.GetProperty<EntityID>(LOCKED_TARGET_ID);
        }
        public static void SetLockedTarget(this Entity entity, EntityID? value)
        {
            entity.SetProperty<EntityID>(LOCKED_TARGET_ID, value);
        }
        #endregion
    }
}