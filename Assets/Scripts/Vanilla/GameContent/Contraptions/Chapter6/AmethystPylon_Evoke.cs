#nullable enable // autogenerated

using System.Collections.Generic;
using MVZ2.GameContent.Effects;
using MVZ2.GameContent.Entities;
using MVZ2.Vanilla.Entities;
using MVZ2.Vanilla.Properties;
using MVZ2Logic.Entities;
using MVZ2Logic.Level;
using PVZEngine;
using PVZEngine.Definitions;
using PVZEngine.Entities;
using PVZEngine.Modifiers;
using Tools;
using UnityEngine;

namespace MVZ2.GameContent.Contraptions
{
    [AutoEntityBehaviourDefinition(VanillaEntityBehaviourNames.amethystPylon_Evoke)]
    public class AmethystPylon_Evoke : ContraptionEvokeBehaviour
    {
        public AmethystPylon_Evoke(string nsp, string name) : base(nsp, name)
        {
        }
        public override void Evoke(Entity entity)
        {
            base.Evoke(entity);
            var sourcePosition = AmethystPylon.GetLaserPosition(entity);
            var param = entity.GetSpawnParams();
            param.SetProperty(EngineEntityProps.FLIP_X, entity.IsFacingLeft());

            entity.Spawn(VanillaEffectID.masterSpark, sourcePosition, param)?.Let(l =>
            {
                AmethystPylon.SetMasterSparkID(entity, new EntityID(l));
                l.SetParent(entity);
                entity.SetEvoked(true);
            });
        }
    }
}
