#nullable enable // autogenerated

using MVZ2.Vanilla.Entities;
using MVZ2Logic.Entities;
using PVZEngine.Definitions;
using PVZEngine.Entities;

namespace MVZ2.GameContent.Entities
{
    [AutoEntityBehaviourDefinition(VanillaEntityBehaviourNames.timeoutRemoveWithoutParent)]
    public class TimeoutRemoveWithoutParentBehaviour : EntityBehaviourDefinition
    {
        public TimeoutRemoveWithoutParentBehaviour(string nsp, string name) : base(nsp, name)
        {
        }
        public override void Init(Entity entity)
        {
            base.Init(entity);
            entity.Timeout = entity.GetMaxTimeout();
        }
        public override void Update(Entity entity)
        {
            base.Update(entity);
            if (!entity.Parent.ExistsAndAlive())
            {
                if (entity.Timeout >= 0)
                {
                    entity.Timeout--;
                    if (entity.Timeout <= 0)
                    {
                        entity.Remove();
                    }
                }
            }
            else
            {
                entity.Timeout = entity.GetMaxTimeout();
            }
        }
    }
}
